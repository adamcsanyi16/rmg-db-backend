<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      ul {
        font-family: Arial, Helvetica, sans-serif;
        list-style-type: none;
      }

      li {
        background-color: antiquewhite;
        box-shadow: 3px 3px 3px lightgrey;
        border-radius: 10px;
        color: rgb(113, 113, 113);
        margin-bottom: 20px;
        padding: 10px;
        width: 95%;
      }
    </style>
    <title>Admin</title>
  </head>
  <body>
    <h1>Felhasználók</h1>
    <ul>
      <% users.forEach(element => { %>
      <li>
        <p>Email: <%= element.email %></p>
        <% if (element.isAdmin) { %>
        <p>Admin: Igen</p>
        <button onclick="{adminvaltoztat}">Admin megváltoztat</button>
        <% } else { %>
        <p>Admin: Nem</p>
        <button onclick="{adminvaltoztat}">Admin megváltoztat</button>
        <% }%> <% })%>
      </li>
    </ul>
  </body>
  <script>
    const adminvaltoztat = () => {};
  </script>
</html>
